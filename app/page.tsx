// app/page.tsx — Main entry point. Renders the full single-page portfolio chat.
// The knowledge base, system prompt, and artifact logic all live in this file
// for easy editing. To update Jia's info, edit the KNOWLEDGE array below.
// To deploy: npm install && npm run dev, then `vercel` to push live.

"use client";
export { default } from "@/components/PortfolioChat";
